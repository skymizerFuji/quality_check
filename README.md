# Installation

clone the repository and set up the virtual environment
```bash
git clone git@git.skymizer.com:product/proprietary/llm/llm-apps/quality_check
uv venv
source .venv/bin/activate
uv sync
```

- Read the readme in each app folder for more details on how to use each app.